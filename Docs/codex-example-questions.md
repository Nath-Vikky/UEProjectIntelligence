# Codex Example Questions

Use these after UEPI MCP connects:

- `先调用 uepi_status，看这个工程当前 Snapshot 是否可查询。`
- `帮我找 BP_ThirdPersonCharacter，并读取它的 Blueprint 节点信息。`
- `BP_ThirdPersonCharacter 的 BeginPlay 静态执行流连接了哪些节点？`
- `MM_Run_Fwd 这段动画有哪些 track、notify、curve 和运动摘要？`
- `这个资产如果改动，可能影响哪些引用方？`
- `如果工具提示 UEPI_REFRESH_REQUESTED，请等编辑器刷新后重试同一个读取。`
